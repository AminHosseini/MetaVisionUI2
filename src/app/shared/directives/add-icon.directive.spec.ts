import { AddIconDirective } from './add-icon.directive';

describe('AddIconDirective', () => {
  it('should create an instance', () => {
    const directive = new AddIconDirective();
    expect(directive).toBeTruthy();
  });
});
