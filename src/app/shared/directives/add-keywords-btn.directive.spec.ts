import { AddKeywordsBtnDirective } from './add-keywords-btn.directive';

describe('AddKeywordsBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new AddKeywordsBtnDirective();
    expect(directive).toBeTruthy();
  });
});
