import { KeywordElementDirective } from './keyword-element.directive';

describe('KeywordElementDirective', () => {
  it('should create an instance', () => {
    const directive = new KeywordElementDirective();
    expect(directive).toBeTruthy();
  });
});
