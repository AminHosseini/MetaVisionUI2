import { DeleteKeywordBtnDirective } from './delete-keyword-btn.directive';

describe('DeleteKeywordBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new DeleteKeywordBtnDirective();
    expect(directive).toBeTruthy();
  });
});
