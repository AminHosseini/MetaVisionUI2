import { FontHelperDirective } from './font-helper.directive';

describe('FontHelperDirective', () => {
  it('should create an instance', () => {
    const directive = new FontHelperDirective();
    expect(directive).toBeTruthy();
  });
});
