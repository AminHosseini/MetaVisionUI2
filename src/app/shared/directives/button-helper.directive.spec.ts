import { ButtonHelperDirective } from './button-helper.directive';

describe('ButtonHelperDirective', () => {
  it('should create an instance', () => {
    const directive = new ButtonHelperDirective();
    expect(directive).toBeTruthy();
  });
});
